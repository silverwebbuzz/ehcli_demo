DB_HOST=localhost
DB_NAME=silverwebbuzz_in_eclinicpro_home
DB_USER=silverwebbuzz_in_eclinicpro_home
DB_PASSWORD=your_password_here

# Set to 1/true/on ONLY in local development to show PHP errors on screen.
# Leave unset (or 0) in production — stack traces must never reach end users.
APP_DEBUG=0
